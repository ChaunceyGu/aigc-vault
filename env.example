# 数据库配置
# 本地开发：使用 localhost:5432（需要本地运行 PostgreSQL）
# Docker 部署：使用 postgres:5432（使用 Docker Compose 中的 postgres 服务，无需暴露端口）
# 外部数据库：使用实际的主机地址和端口
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/aigc_vault
# Docker 部署时请改为：DATABASE_URL=postgresql://postgres:postgres@postgres:5432/aigc_vault

# RustFS/S3 配置
RUSTFS_ENDPOINT_URL=http://localhost:9900
RUSTFS_ACCESS_KEY=your_access_key
RUSTFS_SECRET_KEY=your_secret_key
RUSTFS_BUCKET=aigcvault
RUSTFS_REGION=us-east-1
RUSTFS_USE_SSL=false

# CORS 配置（多个地址用逗号分隔）
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# 日志配置
LOG_LEVEL=INFO

# 文件上传配置（可选）
# MAX_UPLOAD_SIZE=52428800  # 50MB (字节)
# THUMBNAIL_SIZE=300
# THUMBNAIL_QUALITY=85

# 编辑密码配置（可选）
# 如果未设置或设置为空，则创建/编辑记录时不需要密码验证
# 如果设置了密码，创建/编辑/删除记录时需要输入此密码
# 建议设置一个强密码以保护数据安全
EDIT_PASSWORD=your_edit_password_here

